@model Offre
@{
    ViewData["Title"] = "Détails de l'offre";
}

<div class="container mt-4">
    <h2>Détails de la Candidature</h2>

    <div class="card mb-4">
        <div class="card-body">
            <h4>Informations sur l'offre</h4>
            <div class="row">
                <div class="col-md-6 text-dark">
                    <p><strong>Poste :</strong> @Model.Poste</p>
                    <p><strong>Entreprise :</strong> @Model.Recruteur.Nom</p>
                    <p><strong>Type de contrat :</strong> @Model.TypeContrat</p>
                    <p><strong>Secteur :</strong> @Model.Secteur</p>
                </div>
                <div class="col-md-6">
                    <p><strong>Profil requis :</strong> @Model.Profil</p>
                    <p><strong>Rémunération :</strong> @Model.Remuneration €</p>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <h4>Actions</h4>
            <a asp-action="Index" class="btn btn-secondary">Retour à la liste</a>
            <a asp-action="Delete" asp-route-id="@Model.Id"
               class="btn btn-danger"
               onclick="return confirm('Êtes-vous sûr de vouloir annuler cette Offre ?')">
                Annuler l'Offre
            </a>
        </div>
    </div>
</div>